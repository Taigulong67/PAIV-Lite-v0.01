# GitHub 建仓指南 - PAIV-Lite-v0.01

## 操作步骤

### 1. 打开 GitHub 创建页面
访问：https://github.com/new

### 2. 填写仓库信息
| 字段 | 填写内容 |
|------|----------|
| **Repository name** | `PAIV-Lite-v0.01` |
| **Description** | Personal AI Identity & Values Protocol - Lite Version v0.01 |
| **Visibility** | ⭕ Public 或 🔴 Private（你选择） |
| **Initialize this repository with** | ❌ **不要勾选** Add a README |
| | ❌ **不要勾选** Add .gitignore |
| | ❌ **不要勾选** Choose a license |

### 3. 点击创建
点击绿色按钮 **Create repository**

### 4. 告诉我
创建完成后回复我，我会立即推送代码

---

## 创建后的快速设置（可选）

创建完成后，GitHub 会显示类似这样的命令：

```bash
git remote add origin https://github.com/taigulong67/PAIV-Lite-v0.01.git
git branch -M main
git push -u origin main
```

**我已经帮你做好了前两步**，你只需要告诉我仓库创建完成，我执行 `git push` 即可。
